apiVersion: helm.cattle.io/v1
kind: HelmChartConfig
metadata:
  name: traefik
  namespace: kube-system
spec:
  valuesContent: |-
    logs:
      general:
        level: DEBUG
    
    persistence:
      enabled: true
      path: /acme
      size: 128Mi
    
    deployment:
      podSecurityContext:
        fsGroup: 65532

    additionalArguments:
      - "--entrypoints.websecure.http3.advertisedport=443"
      - "--certificatesresolvers.le.acme.tlschallenge=true"
      - "--certificatesresolvers.le.acme.storage=/acme/acme.json"
      - "--certificatesresolvers.le.acme.email=$(TRAEFIK_CERTIFICATESRESOLVERS_LE_ACME_EMAIL)"
      - "--certificatesresolvers.le.acme.caServer=https://acme-v02.api.letsencrypt.org/directory"

    env:
      - name: TRAEFIK_CERTIFICATESRESOLVERS_LE_ACME_EMAIL
        valueFrom:
          secretKeyRef:
            name: traefik-acme-secret
            key: email
    
    ports:
      web:
        redirections:
          entryPoint:
            to: websecure
            scheme: https
            permanent: true
      websecure:
        http3:
          enabled: true
        tls:
          enabled: true

    service:
      enabled: true
      type: LoadBalancer
      ports:
        web:
          port: 80
          protocol: TCP
          expose: true
          targetPort: web
        websecure:
          port: 443
          protocol: TCP
          expose: true
          targetPort: websecure
        websecure-http3:
          port: 443
          protocol: UDP
          expose: true
          targetPort: websecure